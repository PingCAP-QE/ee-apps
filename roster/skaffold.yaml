apiVersion: skaffold/v4beta6
kind: Config
metadata:
  name: roster

build:
  platforms: ["linux/amd64", "linux/arm64"]
  artifacts:
    - image: roster-jobs
      docker:
        dockerfile: Dockerfile.jobs
  local:
    useDockerCLI: true
    useBuildkit: true
    concurrency: 0
