# Dashboard Review Tracker

Last updated: 2026-04-17

This is a living tracker for dashboard review feedback from the current iteration.
When a request changes, or an investigation gets a new conclusion, update the matching item in this file instead of creating a separate ad hoc note.

## Status Legend

- `DONE`: implemented and verified locally
- `INVESTIGATING`: root cause or expected behavior is still being checked
- `PENDING`: agreed to do, but not implemented yet
- `INFO`: explanatory note, no code change required

## Current Items

| ID | Topic | User Request / Question | Current Result | Status | Related Files / Notes |
| --- | --- | --- | --- | --- | --- |
| UI-001 | Flaky default filters | Flaky page should default to `repo=pingcap/tidb`, `branch=master`, `issue_status=closed` | Implemented. Entering `/flaky` now auto-applies these defaults unless the user already changed them. Reset on the Flaky page also returns to these defaults. | `DONE` | `web/src/App.jsx` |
| UI-002 | Default start date | `start_date` default should be the Monday of the week one month ago | Implemented. With current date `2026-04-15`, the default start date resolves to `2026-03-09`. | `DONE` | `web/src/lib/api.js` |
| UI-003 | Filter layout | Global filters were crowded and should support a hidden / expanded second row | Implemented. First row keeps `Start / End / Repo / Branch`. Second row `Job / Cloud / Issue status / Bucket` is collapsible via `More filters` / `Less filters`. | `DONE` | `web/src/components/layout.jsx`, `web/src/styles.css` |
| UI-004 | Issue status parameter | Add an `issue status` filter in the global filter bar | Implemented. Filter supports `All`, `open`, and `closed`. | `DONE` | `web/src/components/layout.jsx`, backend support already present |
| UI-005 | Repo filter options | Repo options should only keep `All repos` and `pingcap/tidb` | Implemented. | `DONE` | `web/src/App.jsx` |
| UI-006 | Branch filter options | Branch options should only keep `All branches`, `main`, `master`, `release-8.5` | Implemented. | `DONE` | `web/src/App.jsx` |
| UI-007 | Distinct flaky case table | Add `Distinct flaky case number` table to the Flaky tab | Implemented. Weekly branch-level table is shown at the top of the Flaky page. | `DONE` | `web/src/pages/FlakyPage.jsx`, `web/src/components/charts.jsx` |
| UI-008 | Issue-filtered weekly metrics | Add issue-filtered weekly case table and issue-filtered flaky rate chart to the Flaky tab | Implemented. Current visible titles use the clearer wording `Filtered-issue weekly case table` and `Filtered-issue flaky rate`, while keeping the same issue-scoped behavior. | `DONE` | `web/src/pages/FlakyPage.jsx`, `web/src/components/charts.jsx` |
| UI-009 | Weekly case table recent-week colors | In `Issue-filtered weekly case table`, the last two weeks should be green if `0%`, red if `>0%` | Implemented. Only the last two week columns are highlighted this way. | `DONE` | `web/src/components/charts.jsx`, `web/src/styles.css` |
| UI-010 | Issue-filtered flaky rate chart height | Reduce chart height by about one third | Implemented. Chart height is reduced to `188`. | `DONE` | `web/src/pages/FlakyPage.jsx` |
| UI-011 | Table freeze behavior | `Issue-filtered weekly case table` first row and first column should stay frozen while scrolling | Implemented. Sticky header and sticky first column are now enabled. | `DONE` | `web/src/styles.css` |
| UI-012 | Table first-column metadata | In the first column of `Issue-filtered weekly case table`, add issue last close time in UTC after status and branch | Implemented. First-column meta now appends a compact `Closed: YYYY-MM-DD HH:mm UTC` label after status and branch when `issue_closed_at` is present; null close time is omitted. | `DONE` | `web/src/components/charts.jsx` |
| UI-013 | Summary badge wording | The old badge `hot in last 2 weeks` was unclear | Implemented. It now shows the number of cases with no flaky signal in the last two weeks. Current wording: `cases no flaky in past 2 weeks`. | `DONE` | `web/src/pages/FlakyPage.jsx` |
| UI-014 | Scope pills / filter context | Make current filter context easier to scan at the top | Implemented. Scope pills are shown under the filter title. | `DONE` | `web/src/components/layout.jsx`, `web/src/styles.css` |
| UI-015 | Flaky summary card naming and denominator display | Rename the three middle Flaky cards to the unified pilot-style terms, and show the lower-left detail as `numerator / failure-like builds` | Implemented using the closest confirmed local terminology from `ci_metrics_sample`: `Flaky Noisy Rate`, `Blind-retry-loop Rate`, and `Combined Noisy Rate`. Their lower-left detail now shows `count / failure_like_build_count`. This is a front-end display update only and does not change backend statistics. | `DONE` | `web/src/pages/FlakyPage.jsx`; Feishu source page itself was not directly readable from the current environment |
| UI-016 | Explain `Unclassified failures` | Clarify the meaning of the last Flaky summary card | Explained. This card is the count of failure-like builds that V1 cannot confidently map to `FLAKY_TEST`, so they remain in the conservative fallback bucket. It is expected to be relatively large until richer signals such as pod events / Jenkins logs are added. | `INFO` | `src/ci_dashboard/api/queries/flaky.py`, `web/src/pages/FlakyPage.jsx` |
| UI-017 | Mark draft charts explicitly | Add `(draft)` to `Failure category trend`, `Period comparison`, and `Failure category share` to show their meaning is not finalized yet | Implemented. `Flaky` now marks all three titles with `(draft)`, and `Overview` also marks the shared `Failure category share` panel the same way for consistency. This is a display-only change. | `DONE` | `web/src/pages/FlakyPage.jsx`, `web/src/pages/OverviewPage.jsx` |
| UI-018 | Blind-retry-loop terminology | Replace visible `Retry-loop` wording with `Blind-retry-loop` consistently | Implemented on the visible dashboard copy paths checked in the current UI: Flaky summary card, composition wording, period comparison label, Overview noisy-job subtitle, and Flaky nav caption. Chart legends backed by `retry_loop_rate_pct` are also labeled as `Blind-retry-loop`. | `DONE` | `web/src/pages/FlakyPage.jsx`, `web/src/pages/OverviewPage.jsx`, `web/src/components/layout.jsx`, `web/src/components/charts.jsx` |
| UI-019 | Flaky lower-section issue filter scope | From the `Flaky Noisy Rate` card downward, the charts should not reference `issue_status`; they should only use repo, branch, job, cloud, and time | Implemented and made explicit. The Flaky page now strips `issue_status` out of the lower job-level queries in backend page assembly, and the UI adds a scope note above the cards so the split is visible while reviewing the page. The issue filter still applies to the two issue-scoped panels above. | `DONE` | `src/ci_dashboard/api/queries/base.py`, `src/ci_dashboard/api/queries/pages.py`, `web/src/pages/FlakyPage.jsx`, `tests/api/test_routes.py` |
| UI-020 | Affected PR Rate card | Add `Affected PR Rate (flaky or blind-retry-loop)` summary card on the Flaky page | Implemented. Period summary now includes `total_pr_count`, `affected_pr_count`, and `affected_pr_rate_pct`, and the Flaky summary cards show `Affected PR Rate` with `affected / total PRs` detail. This lower-section metric follows the same repo / branch / job / cloud / time scope as the other job-level cards and ignores `issue_status`. | `DONE` | `src/ci_dashboard/api/queries/flaky.py`, `web/src/pages/FlakyPage.jsx`, `web/src/styles.css`, `tests/api/test_routes.py` |
| UI-021 | Dense chart x-axis labels | When the selected week/day range is long, reduce x-axis label density so labels do not overlap | Implemented. `TrendChart` now keeps sparse charts unchanged, but when labels are dense it renders every Nth bottom label and always keeps the last label visible. | `DONE` | `web/src/components/charts.jsx` |
| UI-022 | Compact sticky global filters | `Global filters` is too tall; when the page scrolls down it should collapse into a narrower sticky header, and only fully expand when the page is at the very top | Implemented. `FilterBar` now watches page scroll position and switches into a compact sticky state after leaving the top of the page. In compact mode it keeps only the filter summary section above the explanatory note, hides the note itself plus the filter controls, and freezes that summary at the top. Returning to the very top expands the full filter bar again. | `DONE` | `web/src/components/layout.jsx`, `web/src/styles.css` |
| UI-023 | Build Trend default bucket | `Build Trend` tab should default `Bucket` to `week` | Implemented. Entering `/build-trend` now normalizes the shared filter state to `week`, and resetting filters on that tab also returns `Bucket` to `week`. | `DONE` | `web/src/App.jsx` |
| UI-024 | Outcome trend right axis | `Outcome trend` success rate % should use a second Y axis | Implemented. `success_rate_pct` now travels as a right-axis series, and the chart renders it against a dedicated `0-100%` right-side scale instead of mixing it into the left count scale. | `DONE` | `src/ci_dashboard/api/queries/builds.py`, `web/src/components/charts.jsx`, `web/src/pages/BuildTrendPage.jsx`, `tests/api/test_routes.py` |
| UI-025 | Build Trend cloud posture view | Replace `Cloud posture` with weekly IDC vs GCP build-count comparison to show migration volume | Implemented and refined for the `Build Trend` page only. This panel is now titled `Migration status`, keeps the weekly `GCP builds` and `IDC builds` series, and adds a top label on each week showing `GCP / total` as a percentage so migration share is visible at a glance. The Overview page keeps its existing aggregate cloud comparison card. | `DONE` | `src/ci_dashboard/api/queries/builds.py`, `src/ci_dashboard/api/queries/pages.py`, `web/src/pages/BuildTrendPage.jsx`, `web/src/components/charts.jsx`, `web/src/styles.css`, `tests/api/test_routes.py` |
| UI-026 | Repo-share pies by cloud | Add two pie charts to `Build Trend`: repo share on GCP and repo share on IDC | Implemented and refined. `Build Trend` now has a `Build Count Rate grouped by Repo` panel with separate donut charts for `GCP repo share` and `IDC repo share`, both driven by build-count aggregation. This card explicitly ignores the global `cloud` filter, and each donut now shows only the top 10 repos while merging the remainder into a non-drilldown `Others` slice so the chart stays readable under wider scopes. | `DONE` | `src/ci_dashboard/api/queries/base.py`, `src/ci_dashboard/api/queries/builds.py`, `src/ci_dashboard/api/queries/pages.py`, `web/src/pages/BuildTrendPage.jsx`, `web/src/components/charts.jsx`, `web/src/styles.css`, `tests/api/test_routes.py` |
| UI-027 | Pie drilldown to branch mix | Clicking a repo area in the cloud repo-share pie should pop a branch-share pie for that repo | Implemented. Clicking either a donut slice or its legend row opens a modal showing that repo's branch-level build share within the selected cloud. | `DONE` | `web/src/pages/BuildTrendPage.jsx`, `web/src/components/charts.jsx`, `web/src/styles.css` |
| UI-028 | Success-only duration semantics | `run avg time`, `Avg queue wait`, and `Avg total duration` should all only count successful builds because failed builds can end at arbitrary points | Implemented. `Build Trend` duration summaries and duration trend now compute averages from successful builds only, and the top card labels / descriptions make that scope explicit. The same success-only averaging rule is also applied to the shared cloud comparison queue / run / total metrics so the concept stays consistent across pages. | `DONE` | `src/ci_dashboard/api/queries/base.py`, `src/ci_dashboard/api/queries/builds.py`, `web/src/pages/BuildTrendPage.jsx`, `tests/api/test_routes.py` |
| UI-029 | Migration runtime comparison chart | Add a new chart that compares IDC baseline vs recent GCP success `run avg` for the same job, and surfaces `Top 10 improved` plus `Top 10 regressed` jobs | Implemented. `Build Trend` now includes `Migration runtime comparison`, using normalized Jenkins job path as the same-job key across IDC and GCP. The panel compares success-only `run avg` for the 14 days before first successful GCP run versus the latest 14 successful GCP days ending at the selected `end_date`, and renders the result as `Top 10 improved jobs` plus `Top 10 regressed jobs`. This panel intentionally ignores `start_date`, `bucket`, and `cloud` so the migration baseline stays stable. | `DONE` | `docs/ci-dashboard-v1-design.md` section `14.2 Implemented Chart: Job Migration Runtime Comparison`; `src/ci_dashboard/api/queries/builds.py`; `src/ci_dashboard/api/routes/builds.py`; `src/ci_dashboard/api/queries/pages.py`; `web/src/components/charts.jsx`; `tests/api/test_routes.py` |
| UI-030 | Build Trend migration + job rankings | `Migration status` should be about one-third shorter, and `Build Trend` should add one top-10 view for longest `avg success time` jobs plus one for lowest `success rate` jobs | Implemented. `Migration status` now renders at a shorter chart height, and `Build Trend` adds two ranking panels: `Longest avg success time jobs` and `Lowest success rate jobs`. Both rankings follow the current repo / branch / job / cloud / time scope, use success-only avg run time where relevant, and keep build counts visible for interpretation. | `DONE` | `src/ci_dashboard/api/queries/builds.py`, `src/ci_dashboard/api/queries/pages.py`, `web/src/pages/BuildTrendPage.jsx`, `web/src/components/charts.jsx`, `tests/api/test_routes.py` |
| UI-031 | Flaky mixed-chart second Y-axis | `Signal composition` and `Flaky rate trend` line series should use the second Y-axis instead of sharing the count axis with bars | Implemented. These two panels now render failure-like count bars on the left axis and all percentage line series on the right `0-100%` axis, which makes the rate curves readable without distorting bar scale. | `DONE` | `src/ci_dashboard/api/queries/flaky.py`, `web/src/pages/FlakyPage.jsx`, `tests/api/test_routes.py` |
| UI-032 | Build Trend job links | In `Lowest success rate jobs` and `Longest avg success time jobs`, job name should link to the corresponding Jenkins job page | Implemented. Both ranking datasets now carry a `job_url`, and the job names render as clickable external links to the representative Jenkins job page for that job in the current scope. The link host follows the latest matching build for that job, so mixed IDC/GCP history lands on the current visible side instead of a hard-coded host. | `DONE` | `src/ci_dashboard/jobs/build_url_matcher.py`, `src/ci_dashboard/api/queries/builds.py`, `web/src/components/charts.jsx`, `web/src/styles.css`, `tests/api/test_routes.py`, `tests/jobs/test_build_url_matcher.py` |
| UI-033 | Global filter job width | In `Global filters`, the `Job` parameter should have a max width because it overlaps the `Cloud` field | Implemented. The `Job` filter now has its own width cap and the filter controls explicitly allow shrinking within the grid, so long job names no longer spill into the `Cloud` column. | `DONE` | `web/src/components/layout.jsx`, `web/src/styles.css` |
| UI-034 | Build Trend tab rename | Rename the `Build Trend` tab name to `CI Status` | Implemented. The sidebar tab label for route `/build-trend` now displays `CI Status`. Route path and page internals stay unchanged. | `DONE` | `web/src/components/layout.jsx` |
| DATA-001 | Missing `TestAuditPluginRetrying` | Why was `TestAuditPluginRetrying` not visible under `repo=pingcap/tidb`, `branch=master`, `issue_status=closed`? | Root cause identified. The issue exists in `ci_l1_flaky_issues` as issue `#66726`, branch `master`, status `closed`, last close at `2026-03-13 07:59:35 UTC`. Current backend logic excludes closed issues whose `issue_closed_at` is earlier than the selected `start_date`. So this case disappears in recent windows such as `2026-04-02` to `2026-04-15`, but appears when the window starts earlier, such as `2026-03-02`. | `INFO` | `src/ci_dashboard/api/queries/flaky.py` lines around issue scope filtering |
| DATA-002 | Meaning of `issue_status=open` | If `issue_status=open` is selected, does that mean all issue-filtered charts use the set of issues that are open between `start` and `end`? | Clarified for current dashboard behavior. `issue_status` only affects the two issue-scoped panels on the Flaky page: `issue_case_weekly_rates` and `issue_filtered_weekly_trend`. From the `Flaky Noisy Rate` cards downward, all Flaky metrics explicitly ignore `issue_status` and only use repo, branch, job, cloud, and time. The remaining open semantic question is only about how the issue-scoped rows themselves should define `open` / `closed` within a time window. | `INFO` | `src/ci_dashboard/api/queries/flaky.py`, `src/ci_dashboard/api/queries/pages.py`, `tests/api/test_routes.py` |
| DATA-003 | Compare against Feishu table: issue set differences | Under `repo=pingcap/tidb`, `branch=master`, `issue_status=closed`, why do the issue rows differ from the Feishu table? | Investigation narrowed substantially. The full `18 vs 16` row-set mismatch is now explained by deterministic semantic differences: `4` Feishu-only rows are currently reopened (`issue_status=open` now), and the `6` API-only rows come from created-time cutoff differences, missing author filter parity, and one issue that closed after the Feishu snapshot artifact time. The remaining unresolved gap is narrow: we still cannot directly extract the exact Feishu share-anchor block content to prove it is byte-for-byte the same as the local artifact we compared against. | `INVESTIGATING` | See `docs/review-findings/feishu-issue-set-diff.md` |
| DATA-004 | Compare against Feishu table: weekly value differences | Under the same filters, why do the weekly values for the same issue differ from the Feishu table? | Technical root cause is now explained. Across the Feishu-aligned `16 x 6 = 96` cells, denominator mismatches are systematic and collapse into 3 deterministic delta vectors that map directly to current `case_job_scope` composition plus the larger `ci_l1` vs legacy `tmp_*` build coverage gap, especially on week `2026-04-06`. Numerator mismatches are much smaller and concentrated in the latest week, consistent with freshness / snapshot timing. This item is now mainly a product decision about whether dashboard V1 should follow current `ci_l1` system-of-record behavior or preserve legacy `tmp_*` visual parity. | `INFO` | See `docs/review-findings/feishu-weekly-denominator-diff.md` |
| DATA-005 | Flaky / blind-retry-loop cards are too small | `Flaky Noisy Rate` and `Blind-retry-loop Rate` were reporting values like `18 / 17,348` and `9 / 17,348`, far below the pilot / Feishu January baselines | Root cause confirmed in real TiDB data: historical `ci_l1_builds.is_flaky` / `is_retry_loop` flags are stale or missing for many old groups, so the dashboard numerators are undercounted before rendering even starts. Concrete proof: for `pingcap/tidb#65051 / pingcap/tidb/pull_unit_test_next_gen`, the DB had `db_flaky=0, db_retry=0`, but recomputing the same full `(repo, pr, job)` history with current code produced `computed_flaky=1, computed_retry=102`. A one-day real refresh smoke after shrinking flaky-group commit size successfully repaired that sample group in TiDB. Historical execution is now actively running on GKE via Job `apps/ci-dashboard-backfill-20260415080128` started at `2026-04-15T00:01:42Z`; latest observed log moved through flaky chunk `108/210`, and January `pingcap/tidb/master` counts already improved materially to `flaky_builds=1161`, `retry_builds=1915`, `combined_builds=3076`, `affected_prs=197`. Code remediation has landed: flaky refresh now commits in smaller group chunks via `CI_DASHBOARD_REFRESH_GROUP_BATCH_SIZE`, and a lighter `refresh-flaky-signals-range` command also exists for future targeted reruns. Remaining work is to let the current historical run finish and then recheck final dashboard parity. | `INVESTIGATING` | `src/ci_dashboard/jobs/refresh_build_derived.py`, `src/ci_dashboard/jobs/cli.py`, `src/ci_dashboard/common/config.py`, `scripts/render_backfill_job.sh`, `tests/jobs/test_refresh_build_derived.py`, `tests/jobs/test_cli.py`, `tests/test_config_and_db.py` |
| DATA-006 | Full-scope false-positive sweep for `Filtered-issue weekly case table` | After fixing the cross-cloud / stale-build matching bug, run the full current `master` scope and confirm there are no residual false positives | Completed. For `repo=pingcap/tidb`, `branch=master`, `2026-03-02` through `2026-04-17`, both `issue_status=closed` and `issue_status=open` now have `0` cells where the API reports a non-zero flaky count but the same case has no same-week raw `problem_case_runs.flaky=1` evidence. Latest week `2026-04-13` also has `0` false positives. Closed latest-week supported cases are `#67174`, `#66373`, `#67626`, and `#67355`. | `DONE` | `src/ci_dashboard/api/queries/flaky.py`, `tests/api/test_routes.py`, `docs/review-findings/issue-weekly-full-scan-2026-04-17.md` |
| DATA-007 | Old `Distinct flaky case number` artifact mismatch | Why did the older artifact show `master = 196, 133, 102, 140, 143`, while current dashboard now shows `196, 133, 102, 102, 92` / `92` for the later window? | Investigation completed. The old numbers come from the ad hoc artifact `ci_metrics_sample/reports/adhoc_tidb_master/distinct_flaky_case_count_master_release85_weekly_from_20260309.csv`, but there is no checked-in generator script for that file. Current dashboard SQL against current TiDB data produces `196, 133, 102, 102, 92` for the matching scope, and the older `140 / 143` cannot be reproduced from the current source snapshot. Evidence points to two factors: first, the old artifact was produced from an older ad hoc pipeline / data snapshot that is no longer present (`tmp_builds` and `tmp_pr_events` used by `ci_metrics_sample` no longer exist in the current `insight` DB); second, the suspicious `2026-03-30 = 140` inflation matches the signature of an older loose build-key join that omitted both cloud and time disambiguation. The later `2026-04-06 = 143` value still cannot be reconstructed from current data under any consistent current formula, so that part likely depended on the lost old snapshot or manual post-processing. | `INFO` | `docs/review-findings/old-distinct-flaky-count-artifact.md`, `/Users/dillon/workspace/ci_metrics_sample/reports/adhoc_tidb_master/distinct_flaky_case_count_master_release85_weekly_from_20260309.csv`, `/Users/dillon/workspace/ci_metrics_sample/docs/flask_case_update_playbook.md`, `/Users/dillon/workspace/ci_metrics_sample/docs/incremental_data_update_playbook.md` |

## Next Likely Updates

1. Decide whether `issue_status=open/closed` should mean:
   - current latest status only, or
   - status during the selected time window, or
   - issues active at any point in the selected time window.
2. If exact Feishu parity still matters for `DATA-003`, fetch the precise share-anchor block instead of relying on the local comparison artifact.

## Update Rule

When a new conclusion is reached:

1. Update the matching row in `Current Items`.
2. If implementation landed, add the exact file path in `Related Files / Notes`.
3. If behavior changed, replace the old explanation instead of appending conflicting notes.
