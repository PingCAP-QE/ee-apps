---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: macbuilds.build.tibuild.pingcap.net
spec:
  group: build.tibuild.pingcap.net
  names:
    kind: MacBuild
    listKind: MacBuildList
    plural: macbuilds
    singular: macbuild
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: The current build status
      jsonPath: .status.phase
      name: Status
      type: string
    - description: Git Ref
      jsonPath: .spec.gitRef
      name: GitRef
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MacBuild is the Schema for the macbuilds API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MacBuildSpec defines the desired state of MacBuild
            properties:
              artifacts:
                description: Artifacts defines where and how to publish the build
                  output
                properties:
                  push:
                    default: false
                    description: Whether to push artifacts to the registry
                    type: boolean
                  registry:
                    default: hub.pingcap.net
                    description: Target registry (e.g., hub.pingcap.net)
                    type: string
                type: object
              build:
                description: Build defines the parameters for the build process
                properties:
                  arch:
                    description: Architecture to build for (e.g., amd64, arm64)
                    type: string
                  component:
                    description: Component to build (e.g., tidb, tikv)
                    type: string
                  os:
                    default: darwin
                    description: OS to build for.
                    type: string
                  profile:
                    default: release
                    description: Build profile (e.g., release, failpoint)
                    type: string
                  version:
                    description: Version string to release (e.g., v1.2.3, nightly)
                    type: string
                required:
                - component
                - version
                type: object
              source:
                description: Source defines the code to be built
                properties:
                  gitRef:
                    description: Git ref (branch or tag) (e.g., main, v1.0.0)
                    type: string
                  gitRefspec:
                    description: 'Optional: A git refspec to fetch before checkout
                      (e.g., refs/pull/123/head)'
                    type: string
                  gitRepository:
                    description: Git repository URL (e.g., https://github.com/user/repo.git)
                    type: string
                  gitSha:
                    description: |-
                      Optional: A specific git commit SHA to check out. Overrides gitRef if provided.
                      (Maps to Tekton git-sha param)
                    type: string
                required:
                - gitRef
                - gitRepository
                type: object
              ttlSecondsAfterFinished:
                description: |-
                  Seconds to retain the build resource after it has finished (succeeded or failed).
                  After this time, it will be automatically deleted. If unset, it will be kept indefinitely.
                format: int32
                minimum: 0
                type: integer
            required:
            - artifacts
            - build
            - source
            type: object
          status:
            description: MacBuildStatus defines the observed state of MacBuild
            properties:
              commitHash:
                description: The actual Git Commit SHA that was checked out
                type: string
              completionTime:
                description: Timestamp when the build process completed (used for
                  GC)
                format: date-time
                type: string
              message:
                description: A detailed message about the current status (e.g., error
                  log)
                type: string
              outputs:
                description: URLs of the final artifacts after completion
                properties:
                  binaryOciUrl:
                    description: The final OCI URL for the binary, including digest
                    type: string
                  imageOciUrl:
                    description: The final OCI URL for the container image, including
                      digest
                    type: string
                type: object
              phase:
                description: Current phase of the build (Pending, Building, Uploading,
                  Succeeded, Failed)
                enum:
                - Pending
                - Building
                - Uploading
                - Succeeded
                - Failed
                type: string
              startTime:
                description: Timestamp when the build process started
                format: date-time
                type: string
              workerID:
                description: The ID of the macOS worker that is processing this build
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
