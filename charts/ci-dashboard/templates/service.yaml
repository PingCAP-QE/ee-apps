apiVersion: v1
kind: Service
metadata:
  name: {{ include "ci-dashboard.fullname" . }}
  labels:
    {{- include "ci-dashboard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ci-dashboard.selectorLabels" . | nindent 4 }}
