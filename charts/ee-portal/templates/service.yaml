apiVersion: v1
kind: Service
metadata:
  name: {{ include "ee-portal.fullname" . }}
  labels:
    {{- include "ee-portal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "ee-portal.selectorLabels" . | nindent 4 }}
